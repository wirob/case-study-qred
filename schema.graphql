type Query {
  transactions(id: ID!): [Transaction]
  company(id: ID!): Company
}

type Company {
  id: ID!
  name: String!
}

type Transaction {
  date: Float!
  dateFormatted: String!
  amount: Int! 
  id: ID!
}

