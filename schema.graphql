type Query {
  transactions(id: ID!): [Transaction]
  company(id: ID!): Company
}

type Company {
  id: ID!
  name: String!
  transactions: [Transaction]
}

type Transaction {
  date: Float!
  dateFormatted: String!
  amount: Int! 
  id: ID!
}

